# HOW TO INSTALL, COMPILE AND RUN THE CODE : 

1. The SMS_sender app needs to be imported in the android -studio.
2. The Serverconfig.java file needs to be changed and the ip where the server is placed needs to be placed in the base address.
3. The dependency of volley.jar needs to be added to the libs. 
4. The server with the folder named android_api1 needs to be placed at /var/www/ and the apache server needs to be run on the machine with the password of root put in the Config.php file so as to connect with the database server.
5. Now the app can be run in the android-studio.   
 
